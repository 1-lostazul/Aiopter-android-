# Local Nxcode preview identity is injected by the platform.
THREAD_ID=
# Set automatically for a deployed application.
NXCODE_APP_ID=
# Authentication is required by default; false is only for isolated local contract tests.
AUTH_REQUIRED=true
PORT=3001
